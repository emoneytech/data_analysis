.card.card-primary
  .card-header
    %h3.card-title
      %i.fas.fa-exclamation-triangle
      #{@anagrafica.full_name} - 
      = t(:show_resource, resource: EvalMovement.model_name.human)
  .card-body
    %dl.row
      - for attribute in eval_movement.attributes.keys
        %dt.col-md-4.text-right= attribute.humanize
        %dd.col-md-8= eval_movement.attributes[attribute] && eval_movement.attributes[attribute] != '' && eval_movement.attributes[attribute] != '""'? eval_movement.attributes[attribute].to_s : ' - '
  .card-footer
    .row
      .col-md-6
        = link_to 'Prev', customers_anagrafica_eval_movement_path(@anagrafica, @prev) unless @prev.blank?
      .col-md-6.text-right
        = link_to 'Next', customers_anagrafica_eval_movement_path(@anagrafica, @next) unless @next.blank?

= render 'data_analysis/servizi/servizio', servizio: eval_movement.service