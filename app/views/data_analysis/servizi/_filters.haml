.card.card-primary.card-outline{class: "#{ 'collapsed-card' unless params[:filter]}" }
  .card-header
    %h3.card-title
      %i.fa.fa-chart-bar
      = t(:filters, default: 'Filters')
    .card-tools
      %button{type: "button", class: "btn btn-tool", "data-card-widget" => "collapse"}
        %i.fas{class: " #{ params[:filter] ? 'fa-minus' : 'fa-plus'}" }
  .card-body
    = simple_form_for :filter, method: :get, remote: false do |f|
      .row
        .col-md-6= f.input :daterange, label: 'Date range', input_html: { class: 'daterange' }, required: false
        .col-md-6= f.input :product_name, required: false, input_html: {value: "#{params[:filter][:product_name] if params[:filter]}"}
      .row
        .col-md-3= f.input :customer_id, required: false, input_html: {value: "#{params[:filter][:customer_id] if params[:filter]}"}
        .col-md-3= f.input :product_id, required: false, input_html: {value: "#{params[:filter][:product_id] if params[:filter]}"}
        .col-md-3= f.input :vendor, as: :string, required: false, input_html: {value: "#{params[:filter][:vendor] if params[:filter]}"}
      .row
        .col-md-3= f.input :min_amount, required: false, input_html: {value: "#{params[:filter][:min_amount] if params[:filter]}"}
        .col-md-3= f.input :max_amount, required: false, input_html: {value: "#{params[:filter][:max_amount] if params[:filter]}"}

      = f.submit "Submit", disable_with: 'Submiting...', class: 'btn btn-success'
