%table.table.table-striped.table-bordered.table-hover.data-table{id: 'table'}
  %thead
    %tr
      %th= Position.human_attribute_name(:id)
      %th= Position.human_attribute_name(:positionable)
      %th= Position.human_attribute_name(:latitude)
      %th= Position.human_attribute_name(:longitude)
      %th= Position.human_attribute_name(:address)
      %th.action
  %tbody
    - positions.each do |item|
      %tr
        %td= item.id
        %td= item.positionable
        %td= item.latitude
        %td= item.longitude
        %td= item.address
        %td.text-center
          = link_to [:data_analysis, item], class: "btn btn-xs btn-primary" do
            %i.fa.fa-eye
