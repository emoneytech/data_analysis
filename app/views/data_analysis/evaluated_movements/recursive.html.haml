.form-row
  .col-md-12
    =# render 'filters'
#recursions_items.form-row
  = render "recursions", recursions: @recursions
.form-row
  .col-md-12= render "evaluated_movement", evaluated_movement: @evaluated_movement