%table.table.table-striped.table-bordered.table-hover.data-table{id: 'table_movements'}
  %thead
    %tr
      %th= EvaluatedMovement.human_attribute_name(:id)
      %th= EvaluatedMovement.human_attribute_name(:service_id)
      %th= EvaluatedMovement.human_attribute_name(:movement_created_at)
      %th= EvaluatedMovement.human_attribute_name(:customer_full_name)
      %th= EvaluatedMovement.human_attribute_name(:product_name)
      %th= EvaluatedMovement.human_attribute_name(:beneficiary)
      %th= EvaluatedMovement.human_attribute_name(:product_base_risk)
      %th= EvaluatedMovement.human_attribute_name(:recursion)
      %th= EvaluatedMovement.human_attribute_name(:amount)
      %th.action
  %tbody
    - evaluated_movements.each do |item|
      %tr
        %td= item.id
        %td= item.service_id
        %td= item.movement_created_at
        %td= item.customer_full_name
        %td= item.product_name
        %td= item.beneficiary
        %td= item.product_base_risk
        %td= item.recursion
        %td= item.amount
        %td.text-center
          = link_to [:data_analysis, item], class: "btn btn-xs btn-primary" do
            %i.fa.fa-eye
