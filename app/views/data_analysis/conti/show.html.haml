.form-row
  .col-md-8
    = render "statistics", conto: @conto
.form-row
  .col-md-12= render "conto", conto: @conto
- if @movimenticonti
  .form-row= render "data_analysis/movimenticonti/movimenticonti", movimenticonti: @movimenticonti

